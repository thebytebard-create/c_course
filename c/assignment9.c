#include<stdio.h>
int main ()
{   
    /*q1
    float sp,cp,p;
    printf("enter selling and cost price of a product");
    scanf("%f %f",&sp,&cp);
    p=(sp-cp)*100/cp;
    if(p==0)
    {
        printf("no profit or loss");
    }
    else
    {
        if(p>0)
        {
            printf("the profit  is %f %%",p);
        }
        else
        {
            printf("the loss is %f %%",p);
        }  
    }
    return 0;
    */
   
   /*q2
   int a,b,c,d,e;
   printf("enter the marks of five students");
   scanf("%d %d %d %d %d",&a,&b,&c,&d,&e);
   if(a>=33 && b>=33 && c>=33 && d>=33 && e>=33)
   {
    printf("pass");
   }
   else
   {
    printf("fail");
   }

   return 0;
  */
   
  /*q3
   char m;
   printf("enter a character");
   scanf("%c",&m);
   if(m>64 && m<91)
   printf("uppercase");
   else
   printf("lower case");
   return 0;
  */
  
 /*q4
  int x;
  printf("enter a number");
  scanf("%d",&x);
  if(x%2==0 && x%3==0)
  printf("yes");
  else
  printf("no");
  return 0;
 */

/*q5
  int x;
  printf("enter a number");
  scanf("%d",&x);
  if(x%2==0 || x%7==0)
 {
    printf("divisible by 7 or 2");
 }
 else
 {
    
    
        printf("not divisible by any");
    
 }
  return 0;
  */
}